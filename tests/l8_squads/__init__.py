"""L8 squad-gate tests.

``tests/`` is a package in this repository, so this directory is one too --
that keeps module names fully qualified (``tests.l8_squads.test_quorum``) and
makes the intra-package imports in these modules unambiguous.
"""
